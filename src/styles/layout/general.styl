body
    -moz-osx-font-smoothing grayscale
    -webkit-font-smoothing antialiased
    padding-top 52px
    sm(padding-top, 0)

h1, h2, h3, h4, h5, h6, ul
    margin 0
    padding 0

.container
    container()
    sm(max-width, rem(510))
    md(max-width, rem(690))
    lg(max-width, rem(930))
    xl(max-width, rem(1110))